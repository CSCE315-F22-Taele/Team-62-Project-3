<!DOCTYPE html>
<html>
<head>
    <%- include("head") %>
    <title>Summary | Manager</title>
</head>
<body>
    <%- include("header") %>
    <div class="center-content">
        <p>Total Sales: $<%=sales.toLocaleString()%></p>
        <p>Sales this week: $<%=week.toLocaleString()%></p>
                <p>Restock Report: </p>
                <% for(let r of restock) { %>
                    <tr>
                        <td>
                            <p>
                                <%=r.name.toLocaleString()%> needs restock, with <%=r.quantity.toLocaleString()%><%=r.units%> remaining
                            </p>
                        </td>
                    </tr>
                    <% } %>
    </div>
</body>
</html>
